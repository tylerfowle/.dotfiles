#!/usr/bin/env ruby
require 'time'

def time_diff(time1_str, time2_str)
  tin = Time.parse(time2_str)
  tout = Time.parse(time1_str)
  (tin - tout) / 3600
end

puts time_diff(ARGV[0], ARGV[1]) unless ARGV.length != 2
